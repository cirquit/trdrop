import QtQuick 2.0

ListModel {
    ListElement {
        name: "Enable view"
        tooltip: "Renders the exported frame in the main window (Disabling may improve export speed)"
        value: false
    }
}
